version: '3.8'

services:
  temp-mail-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - ./config/domains.json:/app/config/domains.json
    restart: unless-stopped 